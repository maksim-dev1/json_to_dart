# json_to_dart

🎯 **JSON to Dart** — мощный инструмент для быстрого преобразования JSON в Dart/Flutter классы с поддержкой различных опций генерации кода.

## 📋 Описание

Приложение позволяет разработчикам Flutter:
- 📝 Вставлять JSON и моментально получать сгенерированные Dart классы
- ⚙️ Гибко настраивать параметры генерации (Freezed, сериализация, документация и др.)
- 🎨 Удобно редактировать JSON с подсветкой синтаксиса
- 📦 Экспортировать готовый код в буфер обмена

## 🚀 Возможности

### Основные функции
- ✅ **Автоматическая генерация классов** из JSON
- ✅ **Freezed поддержка** — неизменяемые классы с copyWith, ==, hashCode
- ✅ **JSON сериализация** — автоматические методы toJson/fromJson
- ✅ **Настройка типов полей** — DTO, Entity суффиксы
- ✅ **Документация** — генерация doc comments для полей
- ✅ **Форматированный вывод** — красиво оформленный код

### Параметры генерации
| Опция | Описание |
|-------|---------|
| **Use Freezed** | Генерирует неизменяемые классы с Freezed |
| **Use Serialization** | Добавляет toJson/fromJson методы |
| **DTO Suffix** | Добавляет суффикс "DTO" к классам |
| **Entity Suffix** | Добавляет суффикс "Entity" к классам |
| **Add Imports** | Включает необходимые импорты в код |
| **Final Fields** | Делает все поля final |
| **Generate toString** | Добавляет переопределённый toString |
| **Generate copyWith** | Добавляет метод copyWith |
| **Generate Equality** | Добавляет переопределённые == и hashCode |

## 🏗️ Архитектура

### Технологический стек
```
BLoC (State Management)
├── JsonToDartBloc
│   ├── Event: ParseJson, UpdateFilters
│   └── State: Initial, Loading, Success, Failure
│
Presentation Layer
├── Screens: JsonToDartScreen
├── Components:
│   ├── AppBar
│   ├── PanelDivider
│   ├── FilterButton (FAB)
│   ├── FilterDialog (настройки)
│   └── Panels:
│       ├── JsonPanel (редактор JSON)
│       └── ModelsPanel (вывод Dart кода)
│
Domain Layer
└── Entities: FilterConfig (Freezed)

Business Logic
└── JsonParserService (ядро генерации)
```

### Структура папок
```
lib/
├── main.dart                    # Точка входа
├── bootstrap.dart              # Инициализация приложения
├── app.dart                    # Корневой виджет
├── core/
│   └── parser.dart             # Сервис парсинга JSON → Dart
├── theme/
│   └── app_theme.dart          # Темы, стили, цветовая схема
└── futures/json_to_dart/
    ├── domain/
    │   └── enities/
    │       └── filter_config.dart    # Модель конфигурации
    └── presentation/
        ├── bloc/
        │   ├── json_to_dart_bloc.dart
        │   ├── json_to_dart_event.dart
        │   └── json_to_dart_state.dart
        ├── screens/
        │   └── json_to_dart_screen.dart
        ├── components/
        │   ├── app_box.dart          # HBox, WBox утилиты
        │   ├── filter_dialog.dart    # Диалог настроек
        │   ├── git_button.dart       # Кнопка GitHub
        │   ├── resizable_panels.dart # Ресайзируемые панели
        │   └── panels/
        │       ├── json_panel.dart   # Редактор JSON
        │       └── models_panel.dart # Вывод кода
        └── json_to_dart_provider.dart # Provider для BLoC
```

## 📦 Зависимости

### Ключевые пакеты
```yaml
flutter_bloc: ^9.1.1         # State Management
freezed: ^3.2.0              # Immutable classes
re_editor: ^0.7.0            # Code editor с подсветкой
re_highlight: ^0.0.3         # Syntax highlighting
talker_flutter: ^4.9.3       # Логирование
google_fonts: ^6.2.1         # Красивые шрифты
```

## 🎯 Использование

### Установка и запуск

```bash
# Клонирование репозитория
git clone https://github.com/maksim-dev1/json_to_dart.git
cd json_to_dart

# Установка зависимостей
flutter pub get

# Генерация кода Freezed
dart run build_runner build

# Запуск приложения
flutter run
```

### Использование приложения

1. **Вставьте JSON** в левую панель
2. **Нажмите кнопку фильтра** (плавающая кнопка) для настроек
3. **Выберите нужные опции** генерации
4. **Получите готовый код** в правой панели
5. **Скопируйте код** и используйте в проекте

## 🛠️ Особенности реализации

### BLoC Pattern
- **Событие** `ParseJson` — триггер для генерации при изменении JSON
- **Событие** `UpdateFilters` — обновление параметров с регенерацией
- **Состояния** отслеживают Loading, Success и ошибки

### Оптимизация производительности
- ✅ Const конструкторы для виджетов
- ✅ Использование `StatelessWidget` где возможно
- ✅ Правильное разделение на компоненты (избегаем пересоздания виджетов в методах)
- ✅ Ленивая генерация кода только при необходимости

### Качество кода
- ✅ Zero analyzer warnings (анализ пройден)
- ✅ Форматированный код (dart format)
- ✅ Freezed для типобезопасности состояний
- ✅ Отдельные компоненты для переиспользования

## 🔄 Workflow разработки

```
JSON Input → Парсинг → Анализ структуры → Генерация классов → Форматирование → Вывод
                ↓
          UpdateFilters (при изменении опций)
```

## 🎨 Пользовательский интерфейс

### Цветовая схема
- **Основной цвет**: `#1E90FF` (DodgerBlue)
- **Фон**: `#020202` (Почти чёрный)
- **Панели**: `#121212` (Тёмный серый)

### Компоненты UI
- **AppBar** — заголовок с логотипом GitHub
- **Ресайзируемые панели** — JSON редактор и вывод кода
- **Диалог фильтров** — переключатели для опций
- **Подсветка синтаксиса** — JSON и Dart с расцветкой

## 🚀 Развитие проекта

### Планируемые улучшения
- [ ] Поддержка YAML
- [ ] Экспорт в файл
- [ ] История генерации
- [ ] Сохранение шаблонов
- [ ] Темный/светлый режим
- [ ] Мобильная версия

### Известные ограничения
- Максимальный размер JSON: зависит от памяти
- Поддержка Dart 3.8.1+

## 📝 Лицензия

MIT License — смотрите LICENSE файл

## 👨‍💻 Контрибьюции

Приветствуются Pull Requests с улучшениями и исправлениями!

## 🐛 Сообщение об ошибках

Если нашли баг, откройте Issue на GitHub

## 📚 Дополнительные ресурсы

- [Flutter документация](https://docs.flutter.dev)
- [BLoC паттерн](https://bloclibrary.dev)
- [Freezed генератор](https://pub.dev/packages/freezed)
- [JSON сериализация](https://docs.flutter.dev/development/data-and-backend/json)

---

**Версия**: 1.0.0  
**Язык SDK**: Dart 3.8.1+  
**Последнее обновление**: 20 ноября 2025
